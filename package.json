{
  "name": "penixlab-workspace",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev --",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test",
    "backend:dev": "python -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8766"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
